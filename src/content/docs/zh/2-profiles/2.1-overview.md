# 配置方案概览

**配置方案（Profile）描述「在某个网络环境下如何登录」。** 一条配置方案绑定了该网络下的账号、认证地址、网络匹配规则与待执行的登录任务。程序运行时依据当前接入的网络，自动选用最匹配的配置方案。

可将一条配置方案理解为一张校园网账号卡：宿舍 Wi-Fi、教学楼 Wi-Fi、其他校区、不同运营商，均可各自建立一条配置方案，由程序自动切换。

## 内置方案 `default`

程序首次启动会自动创建 `default` 配置方案，它是系统内置的**兜底方案，不可删除**。

- 仅有一个网络环境时，直接编辑 `default` 即可，无需新建。
- 当其他配置方案均不匹配当前网络时，最终回落到 `default`，确保始终存在可用配置。

## 字段说明

| 字段 | 配置键 | 说明 |
| --- | --- | --- |
| 名称 / 标识 | `name` / `id` | `id` 仅新建时可指定，`name` 为任意显示名 |
| 认证地址 | `auth_url` | 门户登录页的完整 URL；劫持型网络下可留空 |
| 触发地址 | `trigger_url` | 仅劫持型网络填写；非空即进入重定向模式，对应控制台开关「重定向模式（劫持型门户）」 |
| 账号 / 密码 | `username` / `password` | 提交后自动加密为 `ENC:` 密文落盘；密码框显示「已保存，清空可更新」 |
| 运营商 | `isp` | 注入为模板变量 `{{ISP}}`，供下拉选择步骤使用；支持输入自定义关键字 |
| 网关 IP | `gateway_ip` | 网络匹配规则之一，详见 [自动匹配网络](2.2-match.md) |
| Wi-Fi 名称 | `wifi_ssid` | 网络匹配规则之一 |
| 活跃任务 | `active_task` | 该网络下待执行的登录任务 ID；不同网络可绑定不同任务，无需为每种网络复制一份任务文件 |

> [!NOTE]
> 账号密码在本地以 `ENC:` 前缀的 AES-256-GCM 密文保存，内存中及时清零，不会以明文上传至任何服务器。

## 控制台操作

进入 **配置方案** 页（`/profiles`）：

| 操作 | 入口 | 说明 |
| --- | --- | --- |
| 编辑 | 配置方案行的 **编辑** | 进入三段式编辑器：基本信息 / 网络匹配 / 认证设置 |
| 设为活跃 | **设为活跃** | 手动指定当前使用的配置方案，顶栏显示其名称 |
| 新建 | **新建配置方案** | 填写 `id`、名称与匹配规则 |
| 删除 | **删除** | `default` 方案的删除请求会被拒绝 |
| 探测当前网络 | 编辑器内 **检测当前网络** | 一键将当前网关 IP / Wi-Fi 名称填入匹配字段 |

> [!NOTE]
> 「活跃任务」需先在 任务管理 页将某个浏览器任务 **设为活跃**（仅浏览器任务可设），配置方案的 `active_task` 即指向该任务。

## 存储位置

每条配置方案独立保存为一个文件：`config/profiles/{id}.json`。删除时移入安全回收站 `config/.trash/`，而非直接抹除。完整目录结构见 [目录与文件](../5-maintenance/5.3-files.md)。

## 后续阅读

- 多条配置方案的选中规则：[自动匹配网络](2.2-match.md)
- 劫持型网络的填写方式：[劫持门户（重定向模式）](2.3-redirect.md)
- 配置方案绑定的任务：[三类任务](../3-tasks/3.1-concepts.md)
